## -*- coding: utf-8 -*-
<%namespace file="base_helper.tmpl" import="*"/>
<!DOCTYPE html>
<html lang="${lang}">
<head>
    ${html_head()}
    <%block name="extra_head">
    </%block>
</head>
<body>
<!-- Menubar -->
<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container-fluid">
            <a class="brand" href="${abs_link('/')}">
            ${blog_title}
            </a>
            <ul class="nav pull-right">
            <%block name="belowtitle">
            %if len(translations) > 1:
                <li>${html_translations()}</li>
            %endif
            </%block>
            <%block name="sourcelink"> </%block>
            </ul>
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span> 
            </a>
            <div class="nav-collapse collapse">
              <ul class="nav">
                ${html_sidebar_links()}
              </ul>
              %if search_form:
                ${search_form}
              %endif
            </div>
        </div>
    </div>
</div>
<!-- End of Menubar -->
<div class="container<%block name="container_width"></%block>" id="container"<%block name="body_container_style"> style="max-width: 960px;"</%block>>
    <!--Body content-->
    <%block name="content"></%block>
    <!--End of body content-->
    <div class="footerbox">
    ${content_footer}
    </div>
</div>
${html_social()}
${analytics}
    <script type="text/javascript">jQuery("a.image-reference").colorbox({rel:"gal",maxWidth:"80%",maxHeight:"80%",scalePhotos:true});</script>
</body>
